/* globally declared types, automatically loaded by typescript. */
interface Window {
  gtag: Function; // google analytics
  /* see <HeatMap /> component */
  __kbase: {
    resizeListenerRegistered?: boolean | undefined;
    resizeHandler?: () => void;
  };
}
